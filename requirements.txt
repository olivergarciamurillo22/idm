# Núcleo
pydantic>=2.7,<3
python-dotenv>=1.0,<2

# Base de datos
sqlalchemy>=2.0,<3
alembic>=1.13,<2

# Documentos
pdfplumber>=0.11,<1
openpyxl>=3.1,<4
xlrd>=2.0,<3
reportlab>=4.1,<5
pillow-heif>=0.16,<2   # HEIC/HEIF de iPhone en Pillow (ruedas para Windows, Mac y Linux)

# Bandeja de revisión (red local)
fastapi>=0.115,<1
starlette>=1.0,<2   # la trae fastapi; se fija para que TestClient siga usando httpx2
uvicorn>=0.29,<1
jinja2>=3.1,<4
python-multipart>=0.0.9

# Desarrollo y pruebas
pytest>=8.2,<9
httpx2>=2.0,<3   # solo para TestClient (starlette>=1.7 lo importa; httpx 0.x es su respaldo y no hace falta)
ruff>=0.4,<1
